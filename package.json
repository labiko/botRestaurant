{
  "name": "bot-restaurant-root",
  "version": "1.0.0",
  "scripts": {
    "build": "cd botResto && npm ci && npm run build:vercel",
    "install": "cd botResto && npm install"
  },
  "engines": {
    "node": ">=18"
  }
}